# inventory_service_api

Generated TypeScript contract/model package.

```sh
make build        # [host] compile the package
make test         # [host] compile and run package tests
make docker-build # [Docker] verify this independent package in Docker
```
