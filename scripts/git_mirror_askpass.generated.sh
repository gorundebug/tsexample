#!/bin/sh
# Code generated by servicegen. DO NOT EDIT.
set -eu

credential_file=${DEPENDENCY_GITHUB_CREDENTIAL_FILE:-}
if [ -z "$credential_file" ] || [ ! -s "$credential_file" ]; then
  exit 0
fi

case "${1:-}" in
  *Username*|*username*) sed -n '1p' "$credential_file" ;;
  *Password*|*password*) sed -n '2p' "$credential_file" ;;
esac