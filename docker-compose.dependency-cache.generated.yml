# Code generated by servicegen. DO NOT EDIT.
name: servicegen-dependency-cache

services:
  git-mirror:
    image: servicegen-git-mirror:2.49.1
    build:
      context: .
      dockerfile: dependency-cache/Dockerfile.git-mirror.generated
    restart: unless-stopped
    ports:
      - "${SERVICEGEN_GIT_MIRROR_BIND_HOST:-127.0.0.1}:${SERVICEGEN_GIT_MIRROR_PORT:-18084}:8080"
    environment:
      SERVICEGEN_GIT_MIRROR_REFRESH_SECONDS: ${SERVICEGEN_GIT_MIRROR_REFRESH_SECONDS:-3600}
    volumes:
      - ${SERVICEGEN_DEPENDENCY_PROXY_DIR:?set SERVICEGEN_DEPENDENCY_PROXY_DIR}/git-mirror:/mirrors
      - ./scripts/git_mirror.generated.cgi:/www/cgi-bin/git:ro

  nexus:
    image: sonatype/nexus3:3.95.0
    restart: unless-stopped
    ports:
      - "${SERVICEGEN_NEXUS_BIND_HOST:-127.0.0.1}:${SERVICEGEN_NEXUS_PORT:-18081}:8081"
      - "${SERVICEGEN_NEXUS_BIND_HOST:-127.0.0.1}:${SERVICEGEN_NEXUS_DOCKER_PORT:-18083}:8082"
    environment:
      INSTALL4J_ADD_VM_PARAMS: >-
        -Xms${SERVICEGEN_NEXUS_HEAP_MIN:-1024m}
        -Xmx${SERVICEGEN_NEXUS_HEAP_MAX:-1024m}
        -XX:MaxDirectMemorySize=${SERVICEGEN_NEXUS_DIRECT_MEMORY:-1024m}
        -Djava.util.prefs.userRoot=/nexus-data/javaprefs
    volumes:
      - ${SERVICEGEN_DEPENDENCY_PROXY_DIR:?set SERVICEGEN_DEPENDENCY_PROXY_DIR}/nexus:/nexus-data