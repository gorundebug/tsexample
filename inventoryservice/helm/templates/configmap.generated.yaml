apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "inventoryservice.fullname" . }}
  labels:
    {{ include "inventoryservice.labels" . | nindent 4 }}
data:
  {{- range $name, $value := .Values.env }}
  {{ $name }}: {{ $value | toString | quote }}
  {{- end }}