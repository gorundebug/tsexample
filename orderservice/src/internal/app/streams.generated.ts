// Code generated by servicegen. DO NOT EDIT.

import type { RuntimeEnvironment } from "@gorundebug/tsservicelib/runtime/graph";
import {
  makeCaseStream, makeDelayStream, makeFilterStream, makeFlatMapIterableStream,
  makeFlatMapStream, makeInputStream, makeJoinStream, makeKeyByStream,
  makeLinkStream, makeMapStream, makeMergeStream, makeMultiJoinLink,
  makeMultiJoinStream, makeProcessStream, makeSinkStream,
  makeSinkStreamWithResult, makeSplitStream, makeSubStream, makeWhenStream,
} from "@gorundebug/tsservicelib/operators";
import type { Order, OrderState } from "../types/index.generated.js";
import type { OrderItem, OrderItemResult, OrderProcessed } from "@gorundebug/model";

import type { ConfigSnapshot } from "../config/config-snapshot.generated.js";
import type { ServiceFunctions } from "./functions.generated.js";

export class ServiceStreams {
  public processOrder!: ReturnType<typeof initProcessOrderStream>;
  public splitPipeline!: ReturnType<typeof initSplitPipelineStream>;
  public processOrderItems!: ReturnType<typeof initProcessOrderItemsStream>;
  public processOrderItem!: ReturnType<typeof initProcessOrderItemStream>["stream"];
  public processOrderItemError!: ReturnType<typeof initProcessOrderItemStream>["errorStream"];
  public mapOrderItemResultToOrderState!: ReturnType<typeof initMapOrderItemResultToOrderStateStream>;
  public softDeadline!: ReturnType<typeof initSoftDeadlineStream>;
  public mapToOrderState!: ReturnType<typeof initMapToOrderStateStream>;
  public mergeResults!: ReturnType<typeof initMergeResultsStream>;
  public splitOrderResult!: ReturnType<typeof initSplitOrderResultStream>;
  public mapToOrderProcessed!: ReturnType<typeof initMapToOrderProcessedStream>;
  public publishOrderProcessed!: ReturnType<typeof initPublishOrderProcessedStream>;

  public initStreams(config: ConfigSnapshot, environment: RuntimeEnvironment, functions: ServiceFunctions): void {
    const initializedProcessOrder = initProcessOrderStream(
      config, environment, functions,
      undefined,
      undefined,
      [],
    );
    this.processOrder = initializedProcessOrder;
    const initializedSplitPipeline = initSplitPipelineStream(
      config, environment, functions,
      this.processOrder,
      undefined,
      [],
    );
    this.splitPipeline = initializedSplitPipeline;
    const initializedProcessOrderItems = initProcessOrderItemsStream(
      config, environment, functions,
      this.splitPipeline.addStream(),
      undefined,
      [],
    );
    this.processOrderItems = initializedProcessOrderItems;
    const initializedProcessOrderItem = initProcessOrderItemStream(
      config, environment, functions,
      this.processOrderItems,
      undefined,
      [],
    );
    this.processOrderItem = initializedProcessOrderItem.stream;
    this.processOrderItemError = initializedProcessOrderItem.errorStream;
    const initializedMapOrderItemResultToOrderState = initMapOrderItemResultToOrderStateStream(
      config, environment, functions,
      this.processOrderItem,
      undefined,
      [],
    );
    this.mapOrderItemResultToOrderState = initializedMapOrderItemResultToOrderState;
    const initializedSoftDeadline = initSoftDeadlineStream(
      config, environment, functions,
      this.splitPipeline.addStream(),
      undefined,
      [],
    );
    this.softDeadline = initializedSoftDeadline;
    const initializedMapToOrderState = initMapToOrderStateStream(
      config, environment, functions,
      this.softDeadline,
      undefined,
      [],
    );
    this.mapToOrderState = initializedMapToOrderState;
    const initializedMergeResults = initMergeResultsStream(
      config, environment, functions,
      undefined,
      undefined,
      [this.mapToOrderState, this.mapOrderItemResultToOrderState, this.processOrderItemError],
    );
    this.mergeResults = initializedMergeResults;
    const initializedSplitOrderResult = initSplitOrderResultStream(
      config, environment, functions,
      this.mergeResults,
      undefined,
      [],
    );
    this.splitOrderResult = initializedSplitOrderResult;
    const initializedMapToOrderProcessed = initMapToOrderProcessedStream(
      config, environment, functions,
      this.splitOrderResult.addStream(),
      undefined,
      [],
    );
    this.mapToOrderProcessed = initializedMapToOrderProcessed;
    const initializedPublishOrderProcessed = initPublishOrderProcessedStream(
      config, environment, functions,
      this.mapToOrderProcessed,
      undefined,
      [],
    );
    this.publishOrderProcessed = initializedPublishOrderProcessed;
  }

  public build(): void {
    this.processOrder.setSource(this.splitOrderResult.addStream());
  }
}

export function initStreams(config: ConfigSnapshot, environment: RuntimeEnvironment, functions: ServiceFunctions): ServiceStreams {
  const streams = new ServiceStreams();
  streams.initStreams(config, environment, functions);
  streams.build();
  return streams;
}
export function initProcessOrderStream(
  config: ConfigSnapshot,
  environment: RuntimeEnvironment,
  functions: ServiceFunctions,
  source?: unknown,
  right?: unknown,
  sources: readonly unknown[] = [],
) {
  const stream = makeInputStream<Order, OrderState, Error>(config.named.streams.processOrder, environment);
  return stream;
}
export function initSplitPipelineStream(
  config: ConfigSnapshot,
  environment: RuntimeEnvironment,
  functions: ServiceFunctions,
  source?: unknown,
  right?: unknown,
  sources: readonly unknown[] = [],
) {
  const stream = makeSplitStream<Order>(config.named.streams.splitPipeline, source as never);
  return stream;
}
export function initProcessOrderItemsStream(
  config: ConfigSnapshot,
  environment: RuntimeEnvironment,
  functions: ServiceFunctions,
  source?: unknown,
  right?: unknown,
  sources: readonly unknown[] = [],
) {
  const stream = makeFlatMapStream<Order, OrderItem>(config.named.streams.processOrderItems, source as never, functions.processOrderItems);
  return stream;
}
export function initProcessOrderItemStream(
  config: ConfigSnapshot,
  environment: RuntimeEnvironment,
  functions: ServiceFunctions,
  source?: unknown,
  right?: unknown,
  sources: readonly unknown[] = [],
) {
  const stream = makeSinkStreamWithResult<OrderItem, OrderItemResult, OrderState>(config.named.streams.processOrderItem, source as never);
  return { stream, errorStream: stream.errorStream() };
}
export function initMapOrderItemResultToOrderStateStream(
  config: ConfigSnapshot,
  environment: RuntimeEnvironment,
  functions: ServiceFunctions,
  source?: unknown,
  right?: unknown,
  sources: readonly unknown[] = [],
) {
  const stream = makeMapStream<OrderItemResult, OrderState>(config.named.streams.mapOrderItemResultToOrderState, source as never, functions.mapOrderItemResultToOrderState);
  return stream;
}
export function initSoftDeadlineStream(
  config: ConfigSnapshot,
  environment: RuntimeEnvironment,
  functions: ServiceFunctions,
  source?: unknown,
  right?: unknown,
  sources: readonly unknown[] = [],
) {
  const stream = makeDelayStream<Order>(config.named.streams.softDeadline, source as never, functions.softDeadline);
  return stream;
}
export function initMapToOrderStateStream(
  config: ConfigSnapshot,
  environment: RuntimeEnvironment,
  functions: ServiceFunctions,
  source?: unknown,
  right?: unknown,
  sources: readonly unknown[] = [],
) {
  const stream = makeMapStream<Order, OrderState>(config.named.streams.mapToOrderState, source as never, functions.mapToOrderState);
  return stream;
}
export function initMergeResultsStream(
  config: ConfigSnapshot,
  environment: RuntimeEnvironment,
  functions: ServiceFunctions,
  source?: unknown,
  right?: unknown,
  sources: readonly unknown[] = [],
) {
  const stream = makeMergeStream<OrderState>(config.named.streams.mergeResults, sources[0] as never, sources[1] as never, sources[2] as never);
  return stream;
}
export function initSplitOrderResultStream(
  config: ConfigSnapshot,
  environment: RuntimeEnvironment,
  functions: ServiceFunctions,
  source?: unknown,
  right?: unknown,
  sources: readonly unknown[] = [],
) {
  const stream = makeSplitStream<OrderState>(config.named.streams.splitOrderResult, source as never);
  return stream;
}
export function initMapToOrderProcessedStream(
  config: ConfigSnapshot,
  environment: RuntimeEnvironment,
  functions: ServiceFunctions,
  source?: unknown,
  right?: unknown,
  sources: readonly unknown[] = [],
) {
  const stream = makeMapStream<OrderState, OrderProcessed>(config.named.streams.mapToOrderProcessed, source as never, functions.mapToOrderProcessed);
  return stream;
}
export function initPublishOrderProcessedStream(
  config: ConfigSnapshot,
  environment: RuntimeEnvironment,
  functions: ServiceFunctions,
  source?: unknown,
  right?: unknown,
  sources: readonly unknown[] = [],
) {
  const stream = makeSinkStream<OrderProcessed, Error>(config.named.streams.publishOrderProcessed, source as never);
  return stream;
}
