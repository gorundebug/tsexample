// Code generated by servicegen. DO NOT EDIT.

import {
  Context, JsonSerde, MessageContext, RuntimeConfigStore,
  SerdeRegistry, SerdeType, ServiceApp, ServiceEnvironment,
  type ServiceAppOptions, type ServiceConfig,
  boolSerdeType, bytesSerdeType, errorSerdeType,
  errorFromUnknown,
  float32SerdeType, float64SerdeType,
  int8SerdeType, int16SerdeType, int32SerdeType, int64SerdeType, intSerdeType,
  makeDefaultSerdeRegistry, makeStreamSerde, runeSerdeType,
  stringSerdeType,
  uint8SerdeType, uint16SerdeType, uint32SerdeType, uint64SerdeType, uintSerdeType,
} from "@gorundebug/tsservicelib/runtime";
import {
  makeCaseStream, makeDelayStream, makeFilterStream, makeFlatMapIterableStream,
  makeFlatMapStream, makeInputStream, makeJoinStream, makeKeyByStream,
  makeLinkStream, makeMapStream, makeMergeStream, makeMultiJoinLink,
  makeMultiJoinStream, makeProcessStream, makeSinkStream,
  makeSinkStreamWithResult, makeSplitStream, makeWhenStream,
} from "@gorundebug/tsservicelib/operators";
import { makeNodeHttpEndpointConsumer as makeNodeHttpSourceEndpointConsumer } from "@gorundebug/tsservicelib/datasource/http";
import { makeKafkaEndpointConsumer as makeKafkaSinkEndpointConsumer } from "@gorundebug/tsservicelib/datasink/kafka";
import {
  makeGrpcNoStreamingEndpointConsumer as makeGrpcNoStreamingSinkEndpointConsumer,
} from "@gorundebug/tsservicelib/datasink/grpc";
import { InventoryServiceApi } from "@gorundebug/inventory-service-api";
import type { Order, OrderState } from "../types/index.generated.js";
import type { OrderItem, OrderItemResult, OrderProcessed } from "@gorundebug/model";
import { Config } from "../config/config.js";
import { DataConnectorIds, ServiceIds, StreamIds } from "../config/config.generated.js";
import {
  MapOrderItemResultToOrderState, makeMapOrderItemResultToOrderState,
  MapToOrderProcessed, makeMapToOrderProcessed,
  MapToOrderState, makeMapToOrderState,
  OrderProcessedEndpoint, makeOrderProcessedEndpoint,
  ProcessOrder, makeProcessOrder,
  ProcessOrderItem, makeProcessOrderItem,
  ProcessOrderItems, makeProcessOrderItems,
  SoftDeadline, makeSoftDeadline,
} from "../functions/index.generated.js";

function isRecord(value: unknown): value is Readonly<Record<string, unknown>> {
  return typeof value === "object" && value !== null && !Array.isArray(value);
}

const serdeTypes = {
  order: new SerdeType<Order>("Order", (value): value is Order => isRecord(value)),
  orderItem: new SerdeType<OrderItem>("OrderItem", (value): value is OrderItem => isRecord(value)),
  orderItemResult: new SerdeType<OrderItemResult>("OrderItemResult", (value): value is OrderItemResult => isRecord(value)),
  orderProcessed: new SerdeType<OrderProcessed>("OrderProcessed", (value): value is OrderProcessed => isRecord(value)),
  orderState: new SerdeType<OrderState>("OrderState", (value): value is OrderState => isRecord(value)),
} as const;

function registerGeneratedSerdes(registry: SerdeRegistry): void {
  registry.register(serdeTypes.order, makeStreamSerde(new JsonSerde(serdeTypes.order)));
  registry.register(serdeTypes.orderItem, makeStreamSerde(new JsonSerde(serdeTypes.orderItem)));
  registry.register(serdeTypes.orderItemResult, makeStreamSerde(new JsonSerde(serdeTypes.orderItemResult)));
  registry.register(serdeTypes.orderProcessed, makeStreamSerde(new JsonSerde(serdeTypes.orderProcessed)));
  registry.register(serdeTypes.orderState, makeStreamSerde(new JsonSerde(serdeTypes.orderState)));
  registry.registerStreamValueType(StreamIds.PROCESS_ORDER, serdeTypes.order);
  registry.registerStreamErrorType(StreamIds.PROCESS_ORDER, errorSerdeType);
  registry.registerStreamValueType(StreamIds.SPLIT_PIPELINE, serdeTypes.order);
  registry.registerStreamValueType(StreamIds.PROCESS_ORDER_ITEMS, serdeTypes.orderItem);
  registry.registerStreamValueType(StreamIds.PROCESS_ORDER_ITEM, serdeTypes.orderItemResult);
  registry.registerStreamErrorType(StreamIds.PROCESS_ORDER_ITEM, serdeTypes.orderState);
  registry.registerStreamValueType(StreamIds.MAP_ORDER_ITEM_RESULT_TO_ORDER_STATE, serdeTypes.orderState);
  registry.registerStreamValueType(StreamIds.SOFT_DEADLINE, serdeTypes.order);
  registry.registerStreamValueType(StreamIds.MAP_TO_ORDER_STATE, serdeTypes.orderState);
  registry.registerStreamValueType(StreamIds.MERGE_RESULTS, serdeTypes.orderState);
  registry.registerStreamValueType(StreamIds.SPLIT_ORDER_RESULT, serdeTypes.orderState);
  registry.registerStreamValueType(StreamIds.MAP_TO_ORDER_PROCESSED, serdeTypes.orderProcessed);
  registry.registerStreamValueType(StreamIds.PUBLISH_ORDER_PROCESSED, serdeTypes.orderProcessed);
  registry.registerStreamErrorType(StreamIds.PUBLISH_ORDER_PROCESSED, errorSerdeType);
}

export interface ServiceMakers {
  mapOrderItemResultToOrderState: (
    context: MessageContext,
    environment: ServiceEnvironment,
    config: import("@gorundebug/tsservicelib/runtime").MapStreamConfig,
  ) => MapOrderItemResultToOrderState;
  mapToOrderProcessed: (
    context: MessageContext,
    environment: ServiceEnvironment,
    config: import("@gorundebug/tsservicelib/runtime").MapStreamConfig,
  ) => MapToOrderProcessed;
  mapToOrderState: (
    context: MessageContext,
    environment: ServiceEnvironment,
    config: import("@gorundebug/tsservicelib/runtime").MapStreamConfig,
  ) => MapToOrderState;
  orderProcessedEndpoint: (
    context: MessageContext,
    environment: ServiceEnvironment,
    config: import("@gorundebug/tsservicelib/runtime").KafkaEndpointConfig,
  ) => OrderProcessedEndpoint;
  processOrder: (
    context: MessageContext,
    environment: ServiceEnvironment,
    config: import("@gorundebug/tsservicelib/runtime").HttpEndpointConfig,
  ) => ProcessOrder;
  processOrderItem: (
    context: MessageContext,
    environment: ServiceEnvironment,
    config: import("@gorundebug/tsservicelib/runtime").GrpcEndpointConfig,
  ) => ProcessOrderItem;
  processOrderItems: (
    context: MessageContext,
    environment: ServiceEnvironment,
    config: import("@gorundebug/tsservicelib/runtime").FlatMapStreamConfig,
  ) => ProcessOrderItems;
  softDeadline: (
    context: MessageContext,
    environment: ServiceEnvironment,
    config: import("@gorundebug/tsservicelib/runtime").DelayStreamConfig,
  ) => SoftDeadline;
}

function defaultMakers(): ServiceMakers {
  return {
    mapOrderItemResultToOrderState: makeMapOrderItemResultToOrderState,
    mapToOrderProcessed: makeMapToOrderProcessed,
    mapToOrderState: makeMapToOrderState,
    orderProcessedEndpoint: makeOrderProcessedEndpoint,
    processOrder: makeProcessOrder,
    processOrderItem: makeProcessOrderItem,
    processOrderItems: makeProcessOrderItems,
    softDeadline: makeSoftDeadline,
  };
}

function initFunctions(
  context: MessageContext,
  config: Config,
  environment: ServiceEnvironment,
  makers: ServiceMakers,
) {
  return {
    mapOrderItemResultToOrderState: makers.mapOrderItemResultToOrderState(context, environment, config.named.streams.mapOrderItemResultToOrderState),
    mapToOrderProcessed: makers.mapToOrderProcessed(context, environment, config.named.streams.mapToOrderProcessed),
    mapToOrderState: makers.mapToOrderState(context, environment, config.named.streams.mapToOrderState),
    orderProcessedEndpoint: makers.orderProcessedEndpoint(context, environment, config.named.endpoints.orderProcessed),
    processOrder: makers.processOrder(context, environment, config.named.endpoints.processOrder),
    processOrderItem: makers.processOrderItem(context, environment, config.named.endpoints.processOrderItem),
    processOrderItems: makers.processOrderItems(context, environment, config.named.streams.processOrderItems),
    softDeadline: makers.softDeadline(context, environment, config.named.streams.softDeadline),
  };
}

export type ServiceFunctions = ReturnType<typeof initFunctions>;

function initClients() {
  return {
  };
}

export type ServiceClients = ReturnType<typeof initClients>;

function initRuntimeConnectors(environment: ServiceEnvironment): void {
  void environment;
}

function initStreams(
  config: Config,
  environment: ServiceEnvironment,
  functions: ServiceFunctions,
) {
  const processOrder = makeInputStream<Order, OrderState, Error>(config.named.streams.processOrder, environment);
  const splitPipeline = makeSplitStream<Order>(config.named.streams.splitPipeline, processOrder);
  const processOrderItems = makeFlatMapStream<Order, OrderItem>(config.named.streams.processOrderItems, splitPipeline.addStream(), functions.processOrderItems);
  const processOrderItem = makeSinkStreamWithResult<OrderItem, OrderItemResult, OrderState>(config.named.streams.processOrderItem, processOrderItems);
  const processOrderItemError = processOrderItem.errorStream();
  const mapOrderItemResultToOrderState = makeMapStream<OrderItemResult, OrderState>(config.named.streams.mapOrderItemResultToOrderState, processOrderItem, functions.mapOrderItemResultToOrderState);
  const softDeadline = makeDelayStream<Order>(config.named.streams.softDeadline, splitPipeline.addStream(), functions.softDeadline);
  const mapToOrderState = makeMapStream<Order, OrderState>(config.named.streams.mapToOrderState, softDeadline, functions.mapToOrderState);
  const mergeResults = makeMergeStream<OrderState>(config.named.streams.mergeResults, mapToOrderState, mapOrderItemResultToOrderState, processOrderItemError);
  const splitOrderResult = makeSplitStream<OrderState>(config.named.streams.splitOrderResult, mergeResults);
  const mapToOrderProcessed = makeMapStream<OrderState, OrderProcessed>(config.named.streams.mapToOrderProcessed, splitOrderResult.addStream(), functions.mapToOrderProcessed);
  const publishOrderProcessed = makeSinkStream<OrderProcessed, Error>(config.named.streams.publishOrderProcessed, mapToOrderProcessed);
  processOrder.setSource(splitOrderResult.addStream());
  return {
    processOrder,
    splitPipeline,
    processOrderItems,
    processOrderItem,
    processOrderItemError,
    mapOrderItemResultToOrderState,
    softDeadline,
    mapToOrderState,
    mergeResults,
    splitOrderResult,
    mapToOrderProcessed,
    publishOrderProcessed,
  };
}

export type ServiceStreams = ReturnType<typeof initStreams>;

function initDataConnectors(
  streams: ServiceStreams,
  functions: ServiceFunctions,
  clients: ServiceClients,
) {
  const [processOrder, processOrderHandler] = makeNodeHttpSourceEndpointConsumer(streams.processOrder, functions.processOrder);
  const processOrderItem = makeGrpcNoStreamingSinkEndpointConsumer(streams.processOrderItem, InventoryServiceApi, InventoryServiceApi.method.processOrderItem, functions.processOrderItem);
  const publishOrderProcessed = makeKafkaSinkEndpointConsumer(streams.publishOrderProcessed, functions.orderProcessedEndpoint);
  return {
    dataConnectors: {
      processOrder,
      processOrderItem,
      publishOrderProcessed,
    },
    handlers: {
      processOrder: processOrderHandler,
    },
  };
}

export type ServiceDataConnectors = ReturnType<typeof initDataConnectors>["dataConnectors"];
export type ServiceHandlers = ReturnType<typeof initDataConnectors>["handlers"];

export abstract class ServiceGenerated {
  protected readonly makers: ServiceMakers = defaultMakers();
  protected clients!: ServiceClients;
  protected functions!: ServiceFunctions;
  protected streams!: ServiceStreams;
  protected dataConnectors!: ServiceDataConnectors;
  protected handlers!: ServiceHandlers;

  protected customMakersInit(_context: MessageContext): void {}
  protected customFunctionsInit(_context: MessageContext): void {}
  protected createServiceAppOptions(_config: ServiceConfig): ServiceAppOptions { return {}; }
  protected onStart(_context: Context): Promise<void> { return Promise.resolve(); }
  protected onStop(_context: Context): Promise<void> { return Promise.resolve(); }

  public async run(arguments_: readonly string[]): Promise<void> {
    const config = await Config.load(arguments_);
    const configStore = new RuntimeConfigStore(config.runtime);
    const configPaths = Config.configPaths(arguments_);
    const serdeRegistry = makeDefaultSerdeRegistry();
    registerGeneratedSerdes(serdeRegistry);
    const serviceConfig = config.runtime.serviceById(ServiceIds.ORDER_SERVICE);
    if (serviceConfig === undefined) {
      throw new Error("service config Order Service not found");
    }
    const app = new ServiceApp(configStore, ServiceIds.ORDER_SERVICE, {
      ...this.createServiceAppOptions(serviceConfig),
      serdeRegistry,
      configReload: {
        valuesPath: configPaths.valuesPath,
        load: async () => (await Config.load(arguments_)).runtime,
      },
    });
    const environment = app.environment();
    const messageContext = new MessageContext();
    this.customMakersInit(messageContext);
    this.clients = initClients();
    this.functions = initFunctions(messageContext, config, environment, this.makers);
    this.customFunctionsInit(messageContext);
    initRuntimeConnectors(environment);
    this.streams = initStreams(config, environment, this.functions);
    const bindings = initDataConnectors(this.streams, this.functions, this.clients);
    this.dataConnectors = bindings.dataConnectors;
    this.handlers = bindings.handlers;

    const context = Context.background();
    await this.onStart(context);
    await app.start(context);
    let terminationError: Error | undefined;
    try {
      terminationError = await waitForTermination();
    } finally {
      await this.onStop(context);
      await app.stop(context, environment.serviceConfig().shutdownTimeout);
    }
    if (terminationError !== undefined) throw terminationError;
  }
}

function waitForTermination(): Promise<Error | undefined> {
  return new Promise((resolve) => {
    const finish = (error?: Error): void => {
      process.off("SIGINT", done);
      process.off("SIGTERM", done);
      process.off("uncaughtException", fail);
      process.off("unhandledRejection", fail);
      resolve(error);
    };
    const done = (): void => {
      finish();
    };
    const fail = (reason: unknown): void => {
      finish(errorFromUnknown(reason));
    };
    process.once("SIGINT", done);
    process.once("SIGTERM", done);
    process.once("uncaughtException", fail);
    process.once("unhandledRejection", fail);
  });
}
