// Code generated by servicegen. DO NOT EDIT.

import type { MessageContext, RuntimeEnvironment } from "@gorundebug/tsservicelib/runtime/graph";
import type { ConfigSnapshot } from "../config/config-snapshot.generated.js";
import type { ServiceMakers } from "./makers.generated.js";
import type {
  ActivityJobEndpointSink,
  ActivityJobEndpointSource,
  FanoutActivityAEndpointSink,
  FanoutActivityAEndpointSource,
  FanoutActivityBEndpointSink,
  FanoutActivityBEndpointSource,
  FanoutActivityCEndpointSink,
  FanoutActivityCEndpointSource,
  SequentialActivityAEndpointSink,
  SequentialActivityAEndpointSource,
  SequentialActivityBEndpointSink,
  SequentialActivityBEndpointSource,
  TemporalActivityScheduleSource,
  ActivityPause,
  ObserveActivityResult,
  ObserveFanoutActivityB,
  ObserveFanoutActivityC,
  ObserveWorkflowResult,
  ProcessActivityJob,
  ProcessFanoutActivityA,
  ProcessFanoutActivityB,
  ProcessFanoutActivityC,
  ProcessScheduledActivity,
  ProcessScheduledWorkflow,
  ProcessSequentialActivityA,
  ProcessSequentialActivityB,
  ProcessWorkflowJob,
  ScheduledActivityPause,
  ScheduledWorkflowPause,
  WorkflowPause,
  LocalScheduleSource,
  FanoutWorkflowJobEndpointSink,
  FanoutWorkflowJobEndpointSource,
  TemporalWorkflowScheduleSource,
  WorkflowJobEndpointSink,
  WorkflowJobEndpointSource,
} from "../functions/index.generated.js";

export class ServiceFunctions {
  public activityJobEndpointSink!: ActivityJobEndpointSink;
  public activityJobEndpointSource!: ActivityJobEndpointSource;
  public fanoutActivityAEndpointSink!: FanoutActivityAEndpointSink;
  public fanoutActivityAEndpointSource!: FanoutActivityAEndpointSource;
  public fanoutActivityBEndpointSink!: FanoutActivityBEndpointSink;
  public fanoutActivityBEndpointSource!: FanoutActivityBEndpointSource;
  public fanoutActivityCEndpointSink!: FanoutActivityCEndpointSink;
  public fanoutActivityCEndpointSource!: FanoutActivityCEndpointSource;
  public sequentialActivityAEndpointSink!: SequentialActivityAEndpointSink;
  public sequentialActivityAEndpointSource!: SequentialActivityAEndpointSource;
  public sequentialActivityBEndpointSink!: SequentialActivityBEndpointSink;
  public sequentialActivityBEndpointSource!: SequentialActivityBEndpointSource;
  public temporalActivityScheduleSource!: TemporalActivityScheduleSource;
  public activityPause!: ActivityPause;
  public observeActivityResult!: ObserveActivityResult;
  public observeFanoutActivityB!: ObserveFanoutActivityB;
  public observeFanoutActivityC!: ObserveFanoutActivityC;
  public observeWorkflowResult!: ObserveWorkflowResult;
  public processActivityJob!: ProcessActivityJob;
  public processFanoutActivityA!: ProcessFanoutActivityA;
  public processFanoutActivityB!: ProcessFanoutActivityB;
  public processFanoutActivityC!: ProcessFanoutActivityC;
  public processScheduledActivity!: ProcessScheduledActivity;
  public processScheduledWorkflow!: ProcessScheduledWorkflow;
  public processSequentialActivityA!: ProcessSequentialActivityA;
  public processSequentialActivityB!: ProcessSequentialActivityB;
  public processWorkflowJob!: ProcessWorkflowJob;
  public scheduledActivityPause!: ScheduledActivityPause;
  public scheduledWorkflowPause!: ScheduledWorkflowPause;
  public workflowPause!: WorkflowPause;
  public localScheduleSource!: LocalScheduleSource;
  public fanoutWorkflowJobEndpointSink!: FanoutWorkflowJobEndpointSink;
  public fanoutWorkflowJobEndpointSource!: FanoutWorkflowJobEndpointSource;
  public temporalWorkflowScheduleSource!: TemporalWorkflowScheduleSource;
  public workflowJobEndpointSink!: WorkflowJobEndpointSink;
  public workflowJobEndpointSource!: WorkflowJobEndpointSource;
}

export async function initFunctions(
  context: MessageContext,
  _config: ConfigSnapshot,
  environment: RuntimeEnvironment,
  makers: ServiceMakers,
): Promise<ServiceFunctions> {
  const functions = new ServiceFunctions();
  {
    const controller = new AbortController();
    const makerContext = context.withExternalCancellation(controller.signal);
    let firstError: unknown;
    let failed = false;
    const invokeMaker = async <T>(maker: () => Promise<T>): Promise<T> => {
      try {
        return await maker();
      } catch (error) {
        if (!failed) {
          failed = true;
          firstError = error;
          controller.abort(error);
        }
        throw error;
      }
    };
    const group = await Promise.allSettled([
      invokeMaker(async () => {
        functions.activityJobEndpointSink = await makers.activityJobEndpointSink(makerContext, environment);
      }),
      invokeMaker(async () => {
        functions.activityJobEndpointSource = await makers.activityJobEndpointSource(makerContext, environment);
      }),
      invokeMaker(async () => {
        functions.fanoutActivityAEndpointSink = await makers.fanoutActivityAEndpointSink(makerContext, environment);
      }),
      invokeMaker(async () => {
        functions.fanoutActivityAEndpointSource = await makers.fanoutActivityAEndpointSource(makerContext, environment);
      }),
      invokeMaker(async () => {
        functions.fanoutActivityBEndpointSink = await makers.fanoutActivityBEndpointSink(makerContext, environment);
      }),
      invokeMaker(async () => {
        functions.fanoutActivityBEndpointSource = await makers.fanoutActivityBEndpointSource(makerContext, environment);
      }),
      invokeMaker(async () => {
        functions.fanoutActivityCEndpointSink = await makers.fanoutActivityCEndpointSink(makerContext, environment);
      }),
      invokeMaker(async () => {
        functions.fanoutActivityCEndpointSource = await makers.fanoutActivityCEndpointSource(makerContext, environment);
      }),
      invokeMaker(async () => {
        functions.sequentialActivityAEndpointSink = await makers.sequentialActivityAEndpointSink(makerContext, environment);
      }),
      invokeMaker(async () => {
        functions.sequentialActivityAEndpointSource = await makers.sequentialActivityAEndpointSource(makerContext, environment);
      }),
      invokeMaker(async () => {
        functions.sequentialActivityBEndpointSink = await makers.sequentialActivityBEndpointSink(makerContext, environment);
      }),
      invokeMaker(async () => {
        functions.sequentialActivityBEndpointSource = await makers.sequentialActivityBEndpointSource(makerContext, environment);
      }),
      invokeMaker(async () => {
        functions.temporalActivityScheduleSource = await makers.temporalActivityScheduleSource(makerContext, environment);
      }),
      invokeMaker(async () => {
        functions.activityPause = await makers.activityPause(makerContext, environment);
      }),
      invokeMaker(async () => {
        functions.observeActivityResult = await makers.observeActivityResult(makerContext, environment);
      }),
      invokeMaker(async () => {
        functions.observeFanoutActivityB = await makers.observeFanoutActivityB(makerContext, environment);
      }),
      invokeMaker(async () => {
        functions.observeFanoutActivityC = await makers.observeFanoutActivityC(makerContext, environment);
      }),
      invokeMaker(async () => {
        functions.observeWorkflowResult = await makers.observeWorkflowResult(makerContext, environment);
      }),
      invokeMaker(async () => {
        functions.processActivityJob = await makers.processActivityJob(makerContext, environment);
      }),
      invokeMaker(async () => {
        functions.processFanoutActivityA = await makers.processFanoutActivityA(makerContext, environment);
      }),
      invokeMaker(async () => {
        functions.processFanoutActivityB = await makers.processFanoutActivityB(makerContext, environment);
      }),
      invokeMaker(async () => {
        functions.processFanoutActivityC = await makers.processFanoutActivityC(makerContext, environment);
      }),
      invokeMaker(async () => {
        functions.processScheduledActivity = await makers.processScheduledActivity(makerContext, environment);
      }),
      invokeMaker(async () => {
        functions.processScheduledWorkflow = await makers.processScheduledWorkflow(makerContext, environment);
      }),
      invokeMaker(async () => {
        functions.processSequentialActivityA = await makers.processSequentialActivityA(makerContext, environment);
      }),
      invokeMaker(async () => {
        functions.processSequentialActivityB = await makers.processSequentialActivityB(makerContext, environment);
      }),
      invokeMaker(async () => {
        functions.processWorkflowJob = await makers.processWorkflowJob(makerContext, environment);
      }),
      invokeMaker(async () => {
        functions.scheduledActivityPause = await makers.scheduledActivityPause(makerContext, environment);
      }),
      invokeMaker(async () => {
        functions.scheduledWorkflowPause = await makers.scheduledWorkflowPause(makerContext, environment);
      }),
      invokeMaker(async () => {
        functions.workflowPause = await makers.workflowPause(makerContext, environment);
      }),
      invokeMaker(async () => {
        functions.localScheduleSource = await makers.localScheduleSource(makerContext, environment);
      }),
      invokeMaker(async () => {
        functions.fanoutWorkflowJobEndpointSink = await makers.fanoutWorkflowJobEndpointSink(makerContext, environment);
      }),
      invokeMaker(async () => {
        functions.fanoutWorkflowJobEndpointSource = await makers.fanoutWorkflowJobEndpointSource(makerContext, environment);
      }),
      invokeMaker(async () => {
        functions.temporalWorkflowScheduleSource = await makers.temporalWorkflowScheduleSource(makerContext, environment);
      }),
      invokeMaker(async () => {
        functions.workflowJobEndpointSink = await makers.workflowJobEndpointSink(makerContext, environment);
      }),
      invokeMaker(async () => {
        functions.workflowJobEndpointSource = await makers.workflowJobEndpointSource(makerContext, environment);
      }),
    ]);
    controller.abort();
    if (failed) throw firstError;
    for (const result of group) {
      if (result.status === "rejected") throw result.reason;
    }
  }
  return functions;
}

export async function initFunctionsParallel(
  context: MessageContext,
  config: ConfigSnapshot,
  environment: RuntimeEnvironment,
  makers: ServiceMakers,
): Promise<ServiceFunctions> {
  return initFunctions(context, config, environment, makers);
}
