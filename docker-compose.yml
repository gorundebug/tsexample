services:
  analyticsservice:
    image: example-typescript-development:latest
    build:
      context: .
      dockerfile: Dockerfile.typescript
      target: ${SERVICEGEN_DOCKER_TARGET:-development}
      extra_hosts:
        - "host.docker.internal:host-gateway"
      args:
        NPM_CONFIG_REGISTRY: ${NPM_CONFIG_REGISTRY:-https://registry.npmjs.org/}
        SERVICEGEN_GITHUB_RAW_URL: ${SERVICEGEN_GITHUB_RAW_URL:-https://github.com}
      additional_contexts:
        tsservicelib-source: "${TSSERVICELIB_SOURCE_CONTEXT:-https://github.com/gorundebug/tsservicelib.git#v0.2.21}"
    command:
      - /workspace/analyticsservice/scripts/run-service.generated.sh
      - /workspace/analyticsservice/src/main.generated.ts
      - --config
      - /workspace/analyticsservice/config/config.yaml
      - --values
      - /workspace/analyticsservice/config/docker_overrides.yaml
    volumes:
      - .:/workspace
      - /workspace/.tsservicelib
      - /workspace/node_modules
      - /workspace/analyticsservice/node_modules
      - /workspace/automationservice/node_modules
      - /workspace/inventoryservice/node_modules
      - /workspace/orderservice/node_modules
      - /workspace/inventory_service_api/node_modules
      - /workspace/inventory_service_api/dist
      - /workspace/model/node_modules
      - /workspace/model/dist
      - /workspace/order_service_api/node_modules
      - /workspace/order_service_api/dist
    restart: unless-stopped
    environment:
      - DEBUG=${ANALYTICS_SERVICE_DEBUG:-0}
      - NODE_DIAGNOSTIC_DIR=/workspace/.artifacts/node-diagnostics/analyticsservice
    ports:
      - "9093:9093"
      - "9203:9203"
      - "2345:2345"
    security_opt:
      - seccomp:unconfined
    cap_add:
      - SYS_PTRACE
    networks:
      - app_net

  automationservice:
    image: example-typescript-development:latest
    build:
      context: .
      dockerfile: Dockerfile.typescript
      target: ${SERVICEGEN_DOCKER_TARGET:-development}
      extra_hosts:
        - "host.docker.internal:host-gateway"
      args:
        NPM_CONFIG_REGISTRY: ${NPM_CONFIG_REGISTRY:-https://registry.npmjs.org/}
        SERVICEGEN_GITHUB_RAW_URL: ${SERVICEGEN_GITHUB_RAW_URL:-https://github.com}
      additional_contexts:
        tsservicelib-source: "${TSSERVICELIB_SOURCE_CONTEXT:-https://github.com/gorundebug/tsservicelib.git#v0.2.21}"
    command:
      - /workspace/automationservice/scripts/run-service.generated.sh
      - /workspace/automationservice/src/main.generated.ts
      - --config
      - /workspace/automationservice/config/config.yaml
      - --values
      - /workspace/automationservice/config/docker_overrides.yaml
    volumes:
      - .:/workspace
      - /workspace/.tsservicelib
      - /workspace/node_modules
      - /workspace/analyticsservice/node_modules
      - /workspace/automationservice/node_modules
      - /workspace/inventoryservice/node_modules
      - /workspace/orderservice/node_modules
      - /workspace/inventory_service_api/node_modules
      - /workspace/inventory_service_api/dist
      - /workspace/model/node_modules
      - /workspace/model/dist
      - /workspace/order_service_api/node_modules
      - /workspace/order_service_api/dist
    restart: unless-stopped
    environment:
      - DEBUG=${AUTOMATION_SERVICE_DEBUG:-0}
      - TEMPORAL_SDK_METRICS_BIND_ADDRESS=0.0.0.0:9464
      - NODE_DIAGNOSTIC_DIR=/workspace/.artifacts/node-diagnostics/automationservice
    ports:
      - "9094:9094"
      - "9204:9204"
      - "2346:2345"
    depends_on:
      temporal-create-namespace:
        condition: service_completed_successfully
    security_opt:
      - seccomp:unconfined
    cap_add:
      - SYS_PTRACE
    networks:
      - app_net

  inventoryservice:
    image: example-typescript-development:latest
    build:
      context: .
      dockerfile: Dockerfile.typescript
      target: ${SERVICEGEN_DOCKER_TARGET:-development}
      extra_hosts:
        - "host.docker.internal:host-gateway"
      args:
        NPM_CONFIG_REGISTRY: ${NPM_CONFIG_REGISTRY:-https://registry.npmjs.org/}
        SERVICEGEN_GITHUB_RAW_URL: ${SERVICEGEN_GITHUB_RAW_URL:-https://github.com}
      additional_contexts:
        tsservicelib-source: "${TSSERVICELIB_SOURCE_CONTEXT:-https://github.com/gorundebug/tsservicelib.git#v0.2.21}"
    command:
      - /workspace/inventoryservice/scripts/run-service.generated.sh
      - /workspace/inventoryservice/src/main.generated.ts
      - --config
      - /workspace/inventoryservice/config/config.yaml
      - --values
      - /workspace/inventoryservice/config/docker_overrides.yaml
    volumes:
      - .:/workspace
      - /workspace/.tsservicelib
      - /workspace/node_modules
      - /workspace/analyticsservice/node_modules
      - /workspace/automationservice/node_modules
      - /workspace/inventoryservice/node_modules
      - /workspace/orderservice/node_modules
      - /workspace/inventory_service_api/node_modules
      - /workspace/inventory_service_api/dist
      - /workspace/model/node_modules
      - /workspace/model/dist
      - /workspace/order_service_api/node_modules
      - /workspace/order_service_api/dist
    restart: unless-stopped
    environment:
      - DEBUG=${INVENTORY_SERVICE_DEBUG:-0}
      - NODE_DIAGNOSTIC_DIR=/workspace/.artifacts/node-diagnostics/inventoryservice
    ports:
      - "9092:9092"
      - "9202:9202"
      - "2347:2345"
    security_opt:
      - seccomp:unconfined
    cap_add:
      - SYS_PTRACE
    networks:
      - app_net

  orderservice:
    image: example-typescript-development:latest
    build:
      context: .
      dockerfile: Dockerfile.typescript
      target: ${SERVICEGEN_DOCKER_TARGET:-development}
      extra_hosts:
        - "host.docker.internal:host-gateway"
      args:
        NPM_CONFIG_REGISTRY: ${NPM_CONFIG_REGISTRY:-https://registry.npmjs.org/}
        SERVICEGEN_GITHUB_RAW_URL: ${SERVICEGEN_GITHUB_RAW_URL:-https://github.com}
      additional_contexts:
        tsservicelib-source: "${TSSERVICELIB_SOURCE_CONTEXT:-https://github.com/gorundebug/tsservicelib.git#v0.2.21}"
    command:
      - /workspace/orderservice/scripts/run-service.generated.sh
      - /workspace/orderservice/src/main.generated.ts
      - --config
      - /workspace/orderservice/config/config.yaml
      - --values
      - /workspace/orderservice/config/docker_overrides.yaml
    volumes:
      - .:/workspace
      - /workspace/.tsservicelib
      - /workspace/node_modules
      - /workspace/analyticsservice/node_modules
      - /workspace/automationservice/node_modules
      - /workspace/inventoryservice/node_modules
      - /workspace/orderservice/node_modules
      - /workspace/inventory_service_api/node_modules
      - /workspace/inventory_service_api/dist
      - /workspace/model/node_modules
      - /workspace/model/dist
      - /workspace/order_service_api/node_modules
      - /workspace/order_service_api/dist
    restart: unless-stopped
    environment:
      - DEBUG=${ORDER_SERVICE_DEBUG:-0}
      - NODE_DIAGNOSTIC_DIR=/workspace/.artifacts/node-diagnostics/orderservice
    ports:
      - "9091:9091"
      - "9201:9201"
      - "2348:2345"
    depends_on:
      analyticsservice:
        condition: service_started
      inventoryservice:
        condition: service_started
    security_opt:
      - seccomp:unconfined
    cap_add:
      - SYS_PTRACE
    networks:
      - app_net


  redpanda:
    image: redpandadata/redpanda:v24.2.5
    restart: unless-stopped
    command:
      - redpanda
      - start
      - --overprovisioned
      - --smp=1
      - --memory=512M
      - --reserve-memory=0M
      - --node-id=0
      - --check=false
      - --kafka-addr=internal://0.0.0.0:9092,external://0.0.0.0:19092
      - --advertise-kafka-addr=internal://redpanda:9092,external://localhost:19092
    ports:
      - "${REDPANDA_PORT:-19092}:19092"
    networks:
      - app_net


  temporal-postgresql:
    image: postgres:16.4-bookworm
    restart: unless-stopped
    environment:
      POSTGRES_USER: temporal
      POSTGRES_PASSWORD: temporal
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U temporal"]
      interval: 5s
      timeout: 5s
      retries: 60
      start_period: 10s
    volumes:
      - temporal_postgresql_data:/var/lib/postgresql/data
    networks:
      - app_net

  temporal-schema:
    image: temporalio/admin-tools:1.31.2
    restart: "no"
    depends_on:
      temporal-postgresql:
        condition: service_healthy
    entrypoint: ["/bin/sh"]
    command: /scripts/setup-postgres.generated.sh
    environment:
      DB_PORT: 5432
      POSTGRES_USER: temporal
      POSTGRES_PWD: temporal
      POSTGRES_SEEDS: temporal-postgresql
    volumes:
      - ./scripts/temporal:/scripts:ro
    networks:
      - app_net

  temporal:
    image: temporalio/server:1.31.2
    restart: unless-stopped
    depends_on:
      temporal-schema:
        condition: service_completed_successfully
    environment:
      DB: postgres12
      DB_PORT: 5432
      POSTGRES_USER: temporal
      POSTGRES_PWD: temporal
      POSTGRES_SEEDS: temporal-postgresql
      BIND_ON_IP: 0.0.0.0
      DYNAMIC_CONFIG_FILE_PATH: config/dynamicconfig/development-sql.generated.yaml
      PROMETHEUS_ENDPOINT: 0.0.0.0:8000
    ports:
      - "${TEMPORAL_PORT:-7233}:7233"
    volumes:
      - ./temporal/dynamicconfig:/etc/temporal/config/dynamicconfig:ro
    healthcheck:
      test: ["CMD", "nc", "-z", "localhost", "7233"]
      interval: 5s
      timeout: 3s
      retries: 60
      start_period: 20s
    networks:
      - app_net

  temporal-create-namespace:
    image: temporalio/admin-tools:1.31.2
    restart: "no"
    depends_on:
      temporal:
        condition: service_healthy
    entrypoint: ["/bin/sh"]
    command: /scripts/create-namespace.generated.sh
    environment:
      DEFAULT_NAMESPACE: default
      TEMPORAL_ADDRESS: temporal:7233
    volumes:
      - ./scripts/temporal:/scripts:ro
    networks:
      - app_net

  temporal-ui:
    image: temporalio/ui:2.53.2
    restart: unless-stopped
    depends_on:
      temporal:
        condition: service_healthy
    environment:
      TEMPORAL_ADDRESS: temporal:7233
      TEMPORAL_CORS_ORIGINS: http://localhost:${TEMPORAL_UI_PORT:-8080}
    ports:
      - "${TEMPORAL_UI_PORT:-8080}:8080"
    networks:
      - app_net

  prometheus:
    image: prom/prometheus:latest
    restart: unless-stopped
    ports:
      - "${PROMETHEUS_PORT:-9090}:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
    networks:
      - app_net

  grafana:
    image: grafana/grafana:latest
    restart: unless-stopped
    ports:
      - "${GRAFANA_PORT:-3000}:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
    volumes:
      - grafana_data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
      - ./analyticsservice/grafana/dist:/etc/grafana/dashboards/analyticsservice:ro
      - ./automationservice/grafana/dist:/etc/grafana/dashboards/automationservice:ro
      - ./inventoryservice/grafana/dist:/etc/grafana/dashboards/inventoryservice:ro
      - ./orderservice/grafana/dist:/etc/grafana/dashboards/orderservice:ro
    networks:
      - app_net

networks:
  app_net:
    driver: bridge

volumes:
  grafana_data:
  temporal_postgresql_data: